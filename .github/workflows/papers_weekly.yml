# ═══════════════════════════════════════════════════════════════
# ⚠️ 已废弃 — 请使用 daily-radar.yml
# ═══════════════════════════════════════════════════════════════
# 此工作流已由 daily-radar.yml 统一整合。
# 保留文件仅作历史参考，可安全删除。
# 最后使用日期: 2026-05-11
# ═══════════════════════════════════════════════════════════════
name: "[DEPRECATED] Paper Radar Weekly"
on:
  workflow_dispatch:  # 仅保留手动触发（用于紧急回滚）

jobs:
  deprecated-warning:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "⚠️ 此工作流已废弃，请使用 daily-radar.yml"
          echo "   手动触发: Actions → Renegade AI Radar → Run workflow"
