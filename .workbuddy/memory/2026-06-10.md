# 2026-06-10 工作日志

## 修复HTML报告显示问题

### 问题1：CSS样式标签缺失
`news_md_to_html.py` 生成的HTML文件中，CSS代码没有被 `<style>` 标签包裹，导致浏览器无法正确解析样式。

**修复**：在 `news_md_to_html.py` 第632行，将 `{CSS_TEMPLATE}` 改为 `<style>{CSS_TEMPLATE}\n  </style>`

### 问题2：索引页无法提取卡片
`card_utils.py` 只识别旧CSS类名(`card`, `card-title`等)，但 `news_md_to_html.py` 生成的是新v5.4类名(`radar-card`, `radar-card-title`等)，导致 `radar_index_generator.py` 无法提取卡片。

**修复**：更新 `card_utils.py` 的 `_extract_with_lxml` 和 `_extract_with_regex` 函数，同时支持新旧类名。

### 问题3：报告页顶部样式缺失
`news_md_to_html.py` 的 `CSS_TEMPLATE` 中缺失多个样式类(`.back-link`, `.page-title`, `.stats-row`, `.urgent-box`等)。

**修复**：添加缺失的CSS样式到 `CSS_TEMPLATE`。

### 问题4：移除不必要的返回链接
报告页中的 `← BACK TO INDEX` 链接不需要。

**修复**：删除 `news_md_to_html.py` 中的返回链接代码。

## 修复结果
- ✅ 重新生成了 `docs/news/news_report_2026-06-10.html`
- ✅ 验证了所有页面显示正常
- ✅ 已提交并推送到GitHub (commits: 0662e03, 146d98f, bf69088, 0156565)

## 自动化保障
所有修复已应用到脚本中，后续每日自动化执行时会自动使用修复后的版本。
- `news_md_to_html.py` - CSS样式标签已修复
- `card_utils.py` - 支持新旧CSS类名
- `news_md_to_html.py` - 报告页样式已完善
- `news_md_to_html.py` - 已移除返回链接

## 第二轮自动化执行 (08:50)
- 📡 抓取163条 → 🔍 预筛选72条 → 🤖 分析72条（62缓存命中 + 10新增）
- ⭐ 24篇高价值（relevance≥7 + case_value=high），其中15篇immediate
- 📄 报告：news_report_2026-06-10.md/.html
- 🚀 GitHub推送成功
- 🆕 10篇新增分析亮点：Claude Fable 5(9分)、MANGOS(7分)、Mythos体验(8分)等

## 学术扫描 pipeline 优化 (09:30)
按新闻雷达同样逻辑排查学术扫描代码，发现以下问题并逐一修复：

### P0 修复（已提交 cfb21fb + 782b463）
1. **S2 搜索无年份过滤** → 默认按相关性排序，"RLHF"/"alignment" 等高频词永远返回2022-2023经典论文
   - `academic_radar.py`: search_semantic_scholar() 新增 `year` 参数（最近2年）
2. **arXiv 无显式日期窗口** → 低频关键词（如 "Darwinian digital evolution"）可能返回数月前论文
   - `academic_radar.py`: search_arxiv() 新增日期过滤（Config.ACADEMIC_DAYS_BACK，默认90天）
   - `config.py`: 新增 ACADEMIC_DAYS_BACK = 90

### P1 修复
3. **每篇分析后全量写缓存** → 240KB 文件重写30-50次/run
   - 改为分析循环结束后一次性 `save_academic_cache(cache)`
4. **缓存命中日志噪音** → 每篇打 INFO，log 累积4367行
   - 改为循环结束后汇总一行：`📦 缓存命中: N 篇, 🆕 待分析: M 篇`
5. **paper_cache.json 死数据** (387KB)
   - `cache.py` 中 load_cache/is_paper_cached/mark_paper_cached/save_draft 从未被调用
   - 重命名为 paper_cache.json.bak，标记函数为 DEPRECATED
   - academic_radar.py 移除死导入

### 缓存架构澄清
- news_radar.py → docs/news/news_cache.json（独立）
- academic_radar.py → docs/academic/academic_cache.json（独立）
- cache.py 的 paper_cache.json 已无消费者，安全归档

### 提交记录
- cfb21fb: perf(academic): 日期过滤 + 批量写缓存 + 清理死代码
- 782b463: fix: 从 Git 跟踪中移除误提交的 paper_cache.json.bak
